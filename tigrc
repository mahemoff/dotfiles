bind status P !git push origin
bind status D !@?rm %(file)
bind generic U !git add --all :/
bind generic R !git reset
bind generic C !git commit -v

# http://stackoverflow.com/a/25975171
bind diff 7 !git diff %(commit)^ %(commit)

# Notes:
# ! to revert (rebase) a file
